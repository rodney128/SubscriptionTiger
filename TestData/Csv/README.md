# Bank CSV Test Data

These CSV files are safe fake samples for manual validation of Bank File Scan behavior.

Purpose:
- monthly subscription pattern
- yearly repeated charge pattern
- yearly keyword single-charge candidate
- debit/credit split handling
- invalid/missing-columns friendly error path

They are intentionally kept outside `SubscriptionTiger/Resources/Raw` so they are not packaged into app builds.
